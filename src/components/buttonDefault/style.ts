import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonDefault: {
    backgroundColor: "#0EA5E9",
    marginBottom: 10,
    padding: 16,
    borderRadius: 10,
  },

  textEnter: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 22,
  },
});
