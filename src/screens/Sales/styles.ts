import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#0F172A",
    padding: 24,
  },

  client: {
    backgroundColor: "#777",
    marginBottom: 35,
    borderRadius: 10,
    padding: 15,
    fontSize: 16,
    color: "#fff",
  },
});
